# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Editor temporaries
*.swp
*.swo
*~
.vscode/
.idea/

# Claude Code: локальные настройки (settings.local.json) остаются вне git,
# а общие — skills/, hooks/, settings.json — шарим в репозитории
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/settings.json

# OS
Thumbs.db
desktop.ini

# Node / тестовый тулинг (devDependencies, не нужны для GitHub Pages)
node_modules/
/test-results/
/playwright-report/
/blob-report/
.playwright/
/coverage/
/screenshots/
